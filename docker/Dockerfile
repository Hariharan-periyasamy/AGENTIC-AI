# Docker configuration placeholder for Phase 13
# The actual Dockerfile will be populated in Phase 13
FROM eclipse-temurin:21-jre
EXPOSE 8080
# COPY target/*.jar app.jar
# ENTRYPOINT ["java","-jar","/app.jar"]
